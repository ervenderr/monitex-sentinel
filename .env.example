# ---- LLM triage ----
# Leave OPENAI_API_KEY unset to run with the deterministic stub provider.
# The pipeline runs end-to-end either way; the dashboard marks stub/degraded triage.
OPENAI_API_KEY=
SENTINEL_LLM_PROVIDER=stub        # stub | openai
SENTINEL_LLM_MODEL=gpt-4o-mini
SENTINEL_LLM_TIMEOUT_S=6.0

# ---- Pipeline ----
SENTINEL_EVENT_STREAM_URL=ws://localhost:8765
SENTINEL_QUEUE_MAXSIZE=512        # bounded: backpressure before shedding
SENTINEL_TRIAGE_WORKERS=4         # concurrent triage workers
SENTINEL_STORE_CAPACITY=1000      # ring buffer of retained events

# ---- API ----
SENTINEL_HOST=127.0.0.1
SENTINEL_PORT=8000
